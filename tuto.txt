
postgreSQL Postgres123!
SQL shell ALTER USER postgres WITH PASSWORD 'Postgres123!';

changer la database postgresSQL de /database:
DATABASE_URL=postgresql://postgres:Postgres123!@localhost:5432/jo_database

pgadmin/ma_database/query_tool

puis python:
pip install -r requirements.txt
pip install python-multipart
 
puis commande: CTRL-C pour quitter 
uvicorn app:app --reload



http://localhost:8000



ALTER TABLE users
    ADD COLUMN first_name VARCHAR(50),
    ADD COLUMN last_name VARCHAR(50);

-- (optionnel) transférer les anciens full_name :
UPDATE users
SET first_name = split_part(full_name, ' ', 1),
    last_name  = split_part(full_name, ' ', 2);